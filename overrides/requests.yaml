kafka:
  controller:
    resources:
      requests:
        memory: 1536Mi
      limits:
        memory: 2Gi

clickhouse:
  clickhouse:
    resources:
      requests:
        memory: 2Gi
      limits:
        memory: 4Gi

sentry:
  ingestConsumer:
    resources:
      requests:
        memory: 256Mi
  cleanup:
    activeDeadlineSeconds: 7200
  worker:
    resources:
      requests:
        memory: 1536Mi

snuba:
  api:
    resources:
      requests:
        memory: 2Gi


zookeeper:
  resources:
    requests:
      memory: 1Gi
