kafka:
  controller:
    resources:
      requests:
        memory: 1536Mi
      limits:
        memory: 2Gi
    persistence:
      size: 16Gi

clickhouse:
  clickhouse:
    resources:
      requests:
        memory: 1536Mi # 1.5Gi
      limits:
        memory: 2Gi
    persistentVolumeClaim:
      dataPersistentVolume:
        storage: 32Gi

postgresql:
  resources:
    requests:
      memory: 3Gi
    limits:
      memory: 3Gi

redis:
  master:
    resources:
      requests:
        memory: 1Gi
      limits:
        memory: 2Gi
  replica:
    resources:
      requests:
        memory: 1Gi
      limits:
        memory: 2Gi

relay:
  resources:
    requests:
      memory: 3584Mi # 3.5Gi
    limits:
      memory: 5Gi

sentry:
  ingestConsumer:
    resources:
      requests:
        memory: 256Mi
  cleanup:
    activeDeadlineSeconds: 7200
  worker:
    resources:
      requests:
        memory: 1792Mi

snuba:
  api:
    resources:
      requests:
        memory: 3Gi


zookeeper:
  resources:
    requests:
      memory: 1Gi

